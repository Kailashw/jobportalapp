#running instruction

* open client-app and running following commands
    ```
        > npm install
        > npm run start
    ```
* open server-app and running following commands
    ```
        > npm install
        > npm run start
    ```